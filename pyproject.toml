[build-system]
requires = ["setuptools>=80.9.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "gitlab-api"
version = "25.14.16"
description = "GitLab API + MCP Server + A2A Server"
readme = "README.md"
authors = [{ name = "Audel Rouhi", email = "knucklessg1@gmail.com" }]
license = { text = "MIT" }
classifiers = [
    "Development Status :: 5 - Production/Stable",
    "License :: Public Domain",
    "Environment :: Console",
    "Operating System :: POSIX :: Linux",
    "Programming Language :: Python :: 3",
]
requires-python = ">=3.10"
dependencies = [
    "requests>=2.8.1",
    "urllib3>=2.2.2",
    "pydantic[email]>=2.8.2",
]
[project.optional-dependencies]
mcp = [
    "fastmcp>=3.0.0b1",
    "eunomia-mcp>=0.3.10",
    "gql>=4.0.0",
    "fastapi>=0.128.0"
]
a2a = [
    "pydantic-ai-slim[fastmcp,openai,anthropic,google,huggingface,a2a,ag-ui,web]>=1.32.0",
    "pydantic-ai-skills",
    "fastapi>=0.128.0"
]
gql = [
    "gql>=4.0.0",
]
all = [
    "fastmcp>=3.0.0b1",
    "eunomia-mcp>=0.3.10",
    "pydantic-ai-slim[fastmcp,openai,anthropic,google,huggingface,a2a,ag-ui,web]>=1.32.0",
    "pydantic-ai-skills",
    "gql>=4.0.0",
    "fastapi>=0.128.0"
]

[project.scripts]
gitlab-mcp = "gitlab_api.gitlab_mcp:gitlab_mcp"
gitlab-agent = "gitlab_api.gitlab_agent:agent_server"

[tool.setuptools.package-data]
gitlab_api = ["mcp_config.json", "skills/**"]

[tool.setuptools.packages.find]
where = ["."]
